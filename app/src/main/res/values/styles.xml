<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:homeAsUpIndicator">@drawable/ic_menu_back</item>
        <item name="colorPrimary">@color/primary</item>
    </style>

    <!--<style name="CardViewStyle" parent="android:Widget.TextView">-->
        <!--<item name="android:foreground">@drawable/card_background</item>-->
    <!--</style>-->

    <style name="CardViewStyle" parent="CardView.Light">
        <item name="android:foreground">?android:attr/selectableItemBackground</item>
    </style>

    <style name="ContextMenuButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/context_button_background</item>
        <item name="android:gravity">left|center_vertical</item>
        <item name="android:paddingLeft">@dimen/margin_normal</item>
        <item name="android:paddingRight">@dimen/margin_normal</item>
        <item name="android:textColor">@color/context_button_text_color</item>
        <item name="android:textSize">@dimen/font_normal</item>
    </style>

    <style name="Dialog" parent="Base.Theme.AppCompat.Light.Dialog">
        <!--<item name="android:windowAnimationStyle">@style/DialogAnimation</item>-->
    </style>

    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@anim/dialog_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_out</item>
    </style>

    <!-- Progress Style -->
    <style name="CircularProgressDrawableStyle" parent="Material.Drawable.CircularProgress"/>

</resources>
